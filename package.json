{
  "name": "cypress-devserver-esbuild",
  "version": "1.0.0",
  "description": "Minimal esbuild dev server for usage with cypress component tests.",
  "main": "./src/dev-server.js",
  "scripts": {
    "test": "cypress run --component"
  },
  "repository": {
    "type": "git",
    "url": "https://github.dev/fochlac/cypress-devserver-esbuild/"
  },
  "files": [
    "src"
  ],
  "keywords": [
    "cypress",
    "esbuild"
  ],
  "author": "Florian Riedel",
  "license": "MIT",
  "peerDependencies": {
    "esbuild": "^0.17.0"
  },
  "devDependencies": {
    "cypress": "^12.10.0",
    "esbuild": "^0.17.18",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "dependencies": {
    "cypress-ct-custom-devserver": "^1.0.1"
  }
}
